#include <stdio.h>
#include <stdlib.h>
#include "paciente.h"

int main() {
    int n;
    printf("Quantos pacientes quer registar? ");
    scanf("%d", &n);

    // TODO: criar vetor de pacientes dinamicamente
    Paciente* lista = NULL;

    // TODO: preencher pacientes
    // preencherPacientes(lista, n);

    // TODO: listar pacientes
    // listarPacientes(lista, n);

    // TODO: filtrar pacientes com glicemia acima de um valor
    // filtrarGlicemia(lista, n, 100.0);

    // TODO: criar cópia da lista e libertar memória
    // Paciente* copia = copiarPacientes(lista, n);
    // libertarPacientes(lista);
    // libertarPacientes(copia);

    return 0;
}
